---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---

<div class={`bg-[#161B22] border border-[#21262D] rounded-xl p-6 ${hover ? 'card-hover cursor-default' : ''} ${className}`}>
  <slot />
</div>
